{
	"name": "websocket-server",
	"version": "1.0.0",
	"description": "web server with sockets",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"sockets"
	],
	"author": "Diego Pagini",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.0.3",
		"express": "^4.18.1"
	}
}
